<body>
    <h1>Subscriber Data for <%= s.name %></h1>
    <table>
        <tr>
            <th>Name</th>
            <td><%= s.name %></td>
        </tr>
        <tr>
            <th>Email</th>
            <td><%= s.email %></td>
        </tr>
        <tr>
            <th>Zip Code</th>
            <td><%= s.zipCode %></td>
    </table>

    <a class="btn btn-primary" href= "/subscribers">View All Subscribers</a>

</body>